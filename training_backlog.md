# Codebase Analysis & Training Plan (Master Backlog Grid)

This document provides a comprehensive analysis of the banking microservices codebase, troubleshooting notes, and the 24 training-ready tasks structured in an exhaustive grid/table format.

---

## 1. Microservice Responsibilities Grid

| Service | Port | Primary Responsibility | Dependencies | Verification Endpoints |
| :--- | :--- | :--- | :--- | :--- |
| **auth-service** | 8004 | Handles user login, session refreshing, token signing/verification | `users-service` | `/api/v1/auth/login` |
| **users-service** | 8003 | Manages identities, profiles, roles, and credential hashes | N/A | `/api/v1/users` |
| **account-service** | 8001 | Manages Savings/Current account balance and state lifecycle | `aadhar-service`, `company-service` | `/api/v1/accounts` |
| **transactions-service** | 8002 | Manages Deposits, Withdrawals, Fund Transfers, Limits, and Logs | `account-service`, `payment-gateway-service` | `/api/v1/transactions` |
| **aadhar-service** | 8005 | Mock UIDAI validation registry for Savings accounts | N/A | `/api/v1/verify` |
| **company-service** | 8006 | Mock Ministry of Corporate Affairs CIN lookup registry | N/A | `/api/v1/company/verify` |
| **payment-gateway-service** | 8009 | Mock central settlement network supporting UPI, IMPS, RTGS, NEFT | N/A | `/api/v1/payment/process` |

---

## 2. Completed Bug Fixes & Troubleshooting Log

| ID | Issue Diagnosed | Impact | Target File | Resolution |
| :--- | :--- | :--- | :--- | :--- |
| **FIX-01** | Authentication failure during credential verification. | Prevented user login. | Database Initializers / Seeds | Aligned password hashes using BCrypt formatting standard. |
| **FIX-02** | React blank page display when opening account overview. | Crashed dashboard. | [AccountsPage.jsx](file:///C:/Users/DELL/OneDrive/Desktop/SRM_FSD/gdb-service/frontend/src/pages/accounts/AccountsPage.jsx#L263) | Added null-safe checking condition for `activated_date` rendering format. |

---

## 3. Master Training Backlog Grid

| Task ID | Level | Type | Module Topic | Service | Target Class / File | Title | Description | Expected Task | UI Expected Outcome |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **MOD1-CR-01** | Beginner | CR | `@Configuration`, `@Bean` | `account-service` | `account-service/src/main/java/com/gdb/account/AccountServiceApplication.java` | Decouple RestTemplate Bean | RestTemplate bean is declared in main boot class. | Extract config to a separate `RestTemplateConfig.java` class. | Compiles cleanly. Downstream current account registration completes with green success toast *"Current Account Created Successfully"*. |
| **MOD1-BUG-01** | Beginner | Bug | Dependency Injection | `account-service` | `account-service/src/main/java/com/gdb/account/service/impl/AccountServiceImpl.java` | NPE on validation helper injection | Helper class lacks `@Component` annotation, causing null injection. | Add `@Component` or `@Service` to `AccountValidator` to register it inside Spring's IoC. | Submitting *"Create Savings Account"* triggers a green success banner instead of a red *"Internal Server Error"* modal, and the new account card appears in the list. |
| **MOD2-CR-01** | Intermediate | CR | Spring AOP, `@Aspect` | `transactions-service` | `transactions-service/src/main/java/com/gdb/transactions/service/impl` | Monitor Performance | Execution metrics are not logged. | Implement a `@Aspect` class recording processing durations on financial impl classes. | Console prints execution duration logs (e.g. `AOP: Completed execution of WithdrawServiceImpl.processWithdraw in 30 ms`); UI successfully displays transaction summary. |
| **MOD2-BUG-01** | Intermediate | Bug | `@Around` Advice | `transactions-service` | `transactions-service/src/main/java/com/gdb/transactions/aspect/LoggingAspect.java` | Double Execution Aspect | Aspect around advice calls `proceed()` twice inside timing calculations. | Modify advice to only execute `joinPoint.proceed()` once, cache, and return it. | Balance increments/decrements by the exact requested amount; only a single transaction entry is logged in the UI history table. |
| **MOD3-CR-01** | Beginner | CR | Spring JDBC | `transactions-service` | `transactions-service/src/main/java/com/gdb/transactions/repository/impl/FundTransferRepositoryImpl.java` | Upgrade to Named Parameters | Queries use positional `?` variables. | Swap `JdbcTemplate` for `NamedParameterJdbcTemplate` and use named parameters. | Transfer forms execute cleanly, displaying the transfer reference ID in a popup success dialog. |
| **MOD3-BUG-01** | Beginner | Bug | Parameter Binding | `transactions-service` | `transactions-service/src/main/java/com/gdb/transactions/repository/impl/FundTransferRepositoryImpl.java` | SQL parameters count mismatch | Query requires two parameters but caller only provides one binding argument. | Bind the correct parameter sequence matching the SQL select parameters count. | Clicking *"Transactions"* or *"Fund Transfer History"* loads the list instantly in a formatted table instead of showing a blank screen or loading spinner. |
| **MOD4-CR-01** | Intermediate | CR | Spring Data JPA | `users-service` | `users-service/src/main/java/com/gdb/users/repository/impl/UserRepositoryImpl.java` | Create Alert Logging Repository | Alerts are not saved to DB. | Implement `SecurityAlertLog` entity and a dynamic standard `SecurityAlertRepository` (JPA). | Instructors can view a new *"Security Alerts Log"* admin grid page on the dashboard to review security breaches. |
| **MOD4-BUG-01** | Intermediate | Bug | Sorting & Pagination | `users-service` | `users-service/src/main/java/com/gdb/users/controller/SecurityAlertController.java` | Mapped Property Sort crash | Sorting query uses database column name (`alert_date`) instead of mapping key (`alertDate`). | Adjust sort parameters in JPA query to reference entity property names. | Clicking column headers correctly re-sorts the table rows dynamically without displaying a blank page or a red error toast. |
| **MOD5-CR-01** | Beginner | CR | Profiles configuration | `transactions-service` | `transactions-service/src/main/resources/application.yml` | Configure Profile Properties | Global hardcoded parameters. | Segment YAML configuration parameters into environment-specific Dev/Prod profiles. | The server starts up in the chosen profile. Logs indicate the profile, and UI constraints adapt (e.g. environment labels or limits). |
| **MOD5-BUG-01** | Intermediate | Bug | `@ConfigurationProperties` | `transactions-service` | `transactions-service/src/main/java/com/gdb/transactions/config/TransferProperties.java` | Limits map to 0 due to name mismatch | `@ConfigurationProperties` field fails mapping due to naming mismatch in properties file. | Realign config mapping names to successfully load max transfer limits. | Replaces the daily limit error popup with a green success checkmark dialog *"Transfer Completed"* for transactions up to $50,000.00. |
| **MOD6-CR-01** | Beginner | CR | DTO Validation | `transactions-service` | `transactions-service/src/main/java/com/gdb/transactions/dto/request/DepositRequest.java` | Introduce Deposit transaction cap | Deposit limit check is missing. | Implement `@DecimalMax` constraint checking on deposit inputs. | Typing a deposit value greater than $50,000.00 highlights the input box in red and shows a validation error banner saying *"Deposits cannot exceed 50,000.00"*. |
| **MOD6-BUG-01** | Beginner | Bug | REST Controllers | `transactions-service` | `transactions-service/src/main/java/com/gdb/transactions/controller/DepositController.java` | Missing Validation on payload | Controller lacks `@Valid` annotation, allowing negative amounts through. | Add `@Valid` to request payload parameters in the controller mapping method. | Negative deposit amounts (`-$1000`) are blocked, and a red validation message *"Amount must be greater than zero"* is shown. |
| **MOD7-CR-01** | Intermediate | CR | Spring Caching | `account-service` | `account-service/src/main/java/com/gdb/account/service/impl/AccountServiceImpl.java` | Configure Cache Management | Repeated DB lookups impact query speed. | Enable cache annotations (`@Cacheable`) on frequently accessed user query interfaces. | Navigating between tabs loads page details instantly without showing reloading spinners. |
| **MOD7-BUG-01** | Intermediate | Bug | Cache Eviction | `account-service` | `account-service/src/main/java/com/gdb/account/service/impl/AccountServiceImpl.java` | Stale data cache desync | Updates bypass cache adjustments, keeping incorrect details on frontend. | Implement `@CacheEvict` or `@CachePut` configurations on write methods. | The dashboard balance updates to the new amount immediately on screen after the transaction completes, without requiring logout or refresh. |
| **MOD8-CR-01** | Advanced | CR | Spring Security | `auth-service` / `transactions-service` | `transactions-service/src/main/java/com/gdb/transactions/security/SecurityFilter.java` | Check Token Blacklist | Tokens remain valid until expiry even after logout. | Call auth-service internally inside the filter to reject blacklisted/revoked tokens. | Logged-out users are immediately redirected back to the login screen, and any unauthorized API calls show a `401 Unauthorized` block. |
| **MOD8-BUG-01** | Intermediate | Bug | Role-based Auth | `users-service` | `users-service/src/main/java/com/gdb/users/security/SecurityUtils.java` | Mismatched operator denies access | Check condition uses `\|\|` instead of `&&` in checking negative parameters, denying everyone. | Correct conditional logic checks inside target authorization classes. | Tellers and Admins can view the client lists and dashboards completely, revealing action buttons to edit, approve, or override. |
| **MOD9-CR-01** | Intermediate | CR | Spring Boot Test | `account-service` | `account-service/src/test/java/com/gdb/account/controller/AccountControllerTest.java` | MockMvc Integration Tests | Code base is lacking tests. | Author mock endpoint tests verifying permissions and valid onboarding workflows. | The test runner compiles successfully and reports green success flags in the IDE/Terminal test panel. |
| **MOD9-BUG-01** | Beginner | Bug | JUnit & Mockito | `account-service` | `account-service/src/test/java/com/gdb/account/client/AadharClientTest.java` | NPE due to missing Mockito Runner | Mock dependencies fail injection, causing NullPointerException. | Add `@ExtendWith(MockitoExtension.class)` annotation at the top of the test class. | Running tests via Maven compile cleanly, displaying the *"BUILD SUCCESS"* console message. |
| **MOD10-CR-01** | Intermediate | CR | API Gateway Pattern | *New Service* | `gateway-service/src/main/resources/application.yml` | Gateway Routing | Port tracking complexity. | Initialize a new gateway-service routing API URLs to core services dynamically. | The React client directs all requests to a single gateway port (8080) and all pages load seamlessly. |
| **MOD10-BUG-01** | Beginner | Bug | Service Port Config | `account-service` | `account-service/src/main/resources/application.yml` | Mismatch on target fallback port | Fallback Aadhar verification calls port `8008` instead of active `8005`. | Modify port assignment values inside default property variables. | Aadhar validation completes successfully (input boxes turn green), and the onboarding wizard completes. |
| **MOD11-CR-01** | Advanced | CR | Resilience4j, Retry | `account-service` | `account-service/src/main/java/com/gdb/account/client/AadharClient.java` | Add Resilience4j Retry | Intermittent connection losses drop registration flow. | Configure a Resilience4j `@Retry` policy on critical rest communication clients. | Downstream connection hiccups do not crash the onboarding flow; the client retries in the background, keeping the user interface active. |
| **MOD11-BUG-01** | Intermediate | Bug | Exception Handler | `account-service` | `account-service/src/main/java/com/gdb/account/client/AadharClient.java` | Swallowed client validation error | Catch block wraps and returns all HTTP failures as internal service outage errors. | Catch and parse client HTTP errors separately to propagate clear validation issues. | The red banner shows the precise validation error (e.g. *"Aadhar number not found in registry"*) instead of generic *"Service Outage"*. |
| **MOD12-CR-01** | Advanced | CR | Eureka Server | *New Service* | `eureka-server/src/main/resources/application.yml` | Service Discovery Registry | Static hosts limit scaling. | Build a Eureka discovery directory server and register microservice endpoints. | Trainees can view the Eureka Dashboard UI (port 8761) showing all microservices listed in green status as *"UP"*. |
| **MOD12-BUG-01** | Intermediate | Bug | Eureka Client | `account-service` | `account-service/src/main/resources/application.yml` | Lookup name naming mismatch | Config references service registry identifier incorrectly, blocking load balancer. | Realign service registration target names inside application configuration properties. | Running under Eureka discovery resolves the auth-service hostname, allowing users to log in successfully without red error toasts. |
